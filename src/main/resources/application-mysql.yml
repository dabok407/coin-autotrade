spring:
  datasource:
    url: jdbc:mysql://localhost:3306/upbitbot?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: upbitbot
    password: upbitbot
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
